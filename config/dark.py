config.isr.doLinearize = False  # we currently have no linearity model
config.isr.doDefect = False  # we currently have no defect models
config.repair.cosmicray.nCrPixelMax = 100000  # darks, by their very nature, tend to have many cosmics
